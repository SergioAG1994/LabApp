"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import type { Session } from "@supabase/supabase-js";
import { supabase } from "@/lib/supabase";

type Entry = {
  id: string; op: string; client: string; samplingNumber: string; sampler: string; quotation: string;
  received: string; due: string; reportNumber: string; analysis: string; sampleNumber: string; sampleId: string;
  analysisOrderCreatedAt: string | null; sampledAt: string | null; issuedAt: string | null; isCancelled: boolean;
};
type OrderRow = { id: string; label: string; unit: string | null; row_type: "result" | "aggregate"; aggregation: string | null; uncertainty: string | null; result_value: string | null; analyst_reference: string | null; result_date: string | null; analyst_name: string | null; released_by: string | null; };

const dash = "—";
const today = () => new Date().toISOString().slice(0, 10);
const formatDate = (value: string | null) => value ? new Intl.DateTimeFormat("es-MX", { day: "2-digit", month: "short", year: "numeric" }).format(new Date(`${value.slice(0, 10)}T12:00:00`)) : dash;
function dueDate(received: string) { const date = new Date(`${received}T12:00:00`); let count = 0; while (count < 8) { date.setDate(date.getDate() + 1); if (date.getDay() !== 0 && date.getDay() !== 6) count += 1; } return date.toISOString().slice(0, 10); }

export default function Home() {
  const [session, setSession] = useState<Session | null>(null); const [authReady, setAuthReady] = useState(false);
  const [authMode, setAuthMode] = useState<"login" | "signup">("login"); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [fullName, setFullName] = useState(""); const [authMessage, setAuthMessage] = useState(""); const [authLoading, setAuthLoading] = useState(false);
  const [view, setView] = useState<"entries" | "new">("entries"); const [entries, setEntries] = useState<Entry[]>([]); const [query, setQuery] = useState(""); const [showCancelled, setShowCancelled] = useState(false); const [selectedEntryIds, setSelectedEntryIds] = useState<Set<string>>(new Set());
  const [client, setClient] = useState(""); const [samplingNumber, setSamplingNumber] = useState(""); const [sampleNumber, setSampleNumber] = useState(""); const [analysis, setAnalysis] = useState("NOM-001-2021-24H"); const [receivedAt, setReceivedAt] = useState(today()); const [sampler, setSampler] = useState(""); const [quotation, setQuotation] = useState(""); const [creating, setCreating] = useState(false);
  const [selected, setSelected] = useState<Entry | null>(null); const [orderRows, setOrderRows] = useState<OrderRow[]>([]); const [sampledInput, setSampledInput] = useState(""); const [savingOrder, setSavingOrder] = useState(false); const [generatingOrder, setGeneratingOrder] = useState(false);
  const orderTableRef = useRef<HTMLDivElement>(null); const orderBottomScrollRef = useRef<HTMLDivElement>(null);
  const [emissionEntry, setEmissionEntry] = useState<Entry | null>(null); const [reportInput, setReportInput] = useState(""); const [issuedInput, setIssuedInput] = useState(""); const [savingEmission, setSavingEmission] = useState(false);

  async function loadEntries() {
    const { data, error } = await supabase.from("analysis_orders").select("id, op_number, status, sampling_number, sampler_name, quotation_number, received_at, sampled_at, due_date, report_number, issued_at, clients(name), analysis_packages(code, name), samples(id, sample_code, analysis_order_created_at)").order("created_at", { ascending: false });
    if (error || !data) return;
    setEntries(data.map((item) => {
      const clientData = (Array.isArray(item.clients) ? item.clients[0] : item.clients) as { name?: string } | null;
      const packageData = (Array.isArray(item.analysis_packages) ? item.analysis_packages[0] : item.analysis_packages) as { code?: string; name?: string } | null;
      const sample = item.samples?.[0];
      return { id: item.id, op: item.op_number, client: clientData?.name || dash, samplingNumber: item.sampling_number || dash, sampler: item.sampler_name || dash, quotation: item.quotation_number || dash, received: formatDate(item.received_at), due: formatDate(item.due_date), reportNumber: item.report_number || "", analysis: packageData?.name || packageData?.code || dash, sampleNumber: sample?.sample_code || dash, sampleId: sample?.id || "", analysisOrderCreatedAt: sample?.analysis_order_created_at || null, sampledAt: item.sampled_at || null, issuedAt: item.issued_at || null, isCancelled: item.status === "cancelada" };
    }));
  }

  useEffect(() => { supabase.auth.getSession().then(({ data: { session: active } }) => { setSession(active); setAuthReady(true); }); const { data: { subscription } } = supabase.auth.onAuthStateChange((_event, active) => { setSession(active); setAuthReady(true); }); return () => subscription.unsubscribe(); }, []);
  useEffect(() => { if (!session) return; const timer = window.setTimeout(() => { void loadEntries(); }, 0); return () => window.clearTimeout(timer); }, [session]);
  const filteredEntries = useMemo(() => entries.filter((entry) => (showCancelled ? entry.isCancelled : !entry.isCancelled) && `${entry.op} ${entry.client} ${entry.sampleNumber} ${entry.samplingNumber} ${entry.analysis}`.toLowerCase().includes(query.toLowerCase())), [entries, query, showCancelled]);

  async function authenticate(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setAuthLoading(true); setAuthMessage(""); const result = authMode === "login" ? await supabase.auth.signInWithPassword({ email, password }) : await supabase.auth.signUp({ email, password, options: { data: { full_name: fullName } } }); setAuthMessage(result.error ? result.error.message : authMode === "login" ? "Acceso correcto." : "Cuenta creada. Revisa tu correo para confirmarla."); setAuthLoading(false); }
  async function createEntry(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setCreating(true); const { error } = await supabase.rpc("create_sample_entry", { p_client_name: client, p_sampling_number: samplingNumber, p_sample_number: sampleNumber, p_package_code: analysis === "Personalizado" ? "PERSONALIZADO" : analysis, p_received_at: receivedAt, p_sampled_at: null, p_sampler_name: sampler || null, p_quotation_number: quotation || null, p_billing_details: null, p_precaptured: false }); setCreating(false); if (error) { window.alert(`No se pudo registrar la entrada: ${error.message}`); return; } setClient(""); setSamplingNumber(""); setSampleNumber(""); setSampler(""); setQuotation(""); setView("entries"); await loadEntries(); }
  async function openSample(entry: Entry) { setSelected(entry); setSampledInput(entry.sampledAt || ""); setOrderRows([]); if (!entry.sampleId) return; const { data } = await supabase.from("analysis_order_rows").select("id, label, unit, row_type, aggregation, uncertainty, result_value, analyst_reference, result_date, analyst_name, released_by").eq("sample_id", entry.sampleId).order("display_order"); setOrderRows((data || []) as OrderRow[]); }
  async function generateAnalysisOrder() { if (!selected?.sampleId) return; setGeneratingOrder(true); const result = selected.analysis.includes("NOM-001-SEMARNAT-2021") ? await supabase.rpc("create_nom001_2021_24h_rows", { p_sample_id: selected.sampleId }) : await supabase.from("samples").update({ analysis_order_created_at: new Date().toISOString() }).eq("id", selected.sampleId); setGeneratingOrder(false); if (result.error) { window.alert(`No se pudo generar la orden: ${result.error.message}`); return; } await loadEntries(); await openSample({ ...selected, analysisOrderCreatedAt: new Date().toISOString() }); }
  function updateRow(id: string, patch: Partial<OrderRow>) { setOrderRows((rows) => rows.map((row) => row.id === id ? { ...row, ...patch } : row)); }
  function syncHorizontalScroll(source: "table" | "bottom", scrollLeft: number) { const target = source === "table" ? orderBottomScrollRef.current : orderTableRef.current; if (target && Math.abs(target.scrollLeft - scrollLeft) > 1) target.scrollLeft = scrollLeft; }
  async function saveAnalysisOrder() { if (!selected) return; setSavingOrder(true); const dateResult = await supabase.from("analysis_orders").update({ sampled_at: sampledInput || null }).eq("id", selected.id); const results = await Promise.all(orderRows.map((row) => supabase.from("analysis_order_rows").update({ uncertainty: row.uncertainty === "" ? null : row.uncertainty, result_value: row.result_value?.trim() || null, analyst_reference: row.analyst_reference?.trim() || null, result_date: row.result_date || null, analyst_name: row.analyst_name?.trim().toUpperCase() || null, released_by: row.released_by?.trim().toUpperCase() || null, updated_at: new Date().toISOString() }).eq("id", row.id))); setSavingOrder(false); const error = dateResult.error || results.find((result) => result.error)?.error; if (error) { window.alert(`No se pudo guardar la orden: ${error.message}`); return; } await loadEntries(); setSelected({ ...selected, sampledAt: sampledInput || null }); window.alert("Orden de análisis guardada."); }
  function openEmission(entry: Entry) { setEmissionEntry(entry); setReportInput(entry.reportNumber); setIssuedInput(entry.issuedAt || ""); }
  async function saveEmission(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (!emissionEntry) return; setSavingEmission(true); const { error } = await supabase.from("analysis_orders").update({ report_number: reportInput.trim() || null, issued_at: issuedInput || null }).eq("id", emissionEntry.id); setSavingEmission(false); if (error) { window.alert(`No se pudieron guardar los datos de emisión: ${error.message}`); return; } setEmissionEntry(null); await loadEntries(); }
  function toggleEntry(id: string) { setSelectedEntryIds((current) => { const next = new Set(current); if (next.has(id)) next.delete(id); else next.add(id); return next; }); }
  function toggleAll() { setSelectedEntryIds((current) => current.size === filteredEntries.length ? new Set() : new Set(filteredEntries.map((entry) => entry.id))); }
  async function runSelection(action: "cancel_sample_entry" | "restore_sample_entry" | "delete_sample_entry_permanently", confirmation: string) { if (!selectedEntryIds.size || !window.confirm(confirmation)) return; const results = await Promise.all([...selectedEntryIds].map((id) => supabase.rpc(action, { p_order_id: id }))); const error = results.find((result) => result.error)?.error; if (error) { window.alert(error.message); return; } setSelectedEntryIds(new Set()); await loadEntries(); }

  if (!authReady) return <main className="auth-page"><p>Conectando con LabAqua…</p></main>;
  if (!session) return <main className="auth-page"><section className="auth-card"><div className="auth-brand"><span>LA</span><div><strong>LabAqua</strong><small>Control de análisis</small></div></div><p className="eyebrow">ACCESO INTERNO</p><h1>{authMode === "login" ? "Bienvenido de nuevo" : "Crear cuenta de laboratorio"}</h1><p className="auth-description">{authMode === "login" ? "Ingresa con tu cuenta autorizada." : "Crea la primera cuenta para probar el sistema."}</p><form onSubmit={authenticate} className="auth-form">{authMode === "signup" && <label>Nombre completo<input required value={fullName} onChange={(event) => setFullName(event.target.value)} /></label>}<label>Correo electrónico<input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} /></label><label>Contraseña<input required type="password" minLength={6} value={password} onChange={(event) => setPassword(event.target.value)} /></label>{authMessage && <p className="auth-message">{authMessage}</p>}<button className="button primary full" disabled={authLoading}>{authLoading ? "Procesando…" : authMode === "login" ? "Ingresar" : "Crear cuenta"}</button></form><button className="auth-switch" onClick={() => { setAuthMode(authMode === "login" ? "signup" : "login"); setAuthMessage(""); }}>{authMode === "login" ? "¿No tienes cuenta? Crear cuenta" : "¿Ya tienes cuenta? Iniciar sesión"}</button></section></main>;

  return <main className="app-shell"><aside className="sidebar"><div className="brand"><span>LA</span><div><strong>LabAqua</strong><small>Control de análisis</small></div></div><nav><button className={!showCancelled && view === "entries" ? "nav-item active" : "nav-item"} onClick={() => { setView("entries"); setShowCancelled(false); setSelectedEntryIds(new Set()); }}>▦ &nbsp; Entrada de muestras</button><button className={showCancelled ? "nav-item nav-subitem active" : "nav-item nav-subitem"} onClick={() => { setView("entries"); setShowCancelled(true); setSelectedEntryIds(new Set()); }}>↳ &nbsp; OPs eliminados</button><button className="nav-item muted">◫ &nbsp; Informes</button><button className="nav-item muted">▥ &nbsp; Reportes</button></nav><button className="user-card" onClick={() => supabase.auth.signOut()}><div className="avatar">{(session.user.email?.slice(0, 2) || "US").toUpperCase()}</div><div><strong>{session.user.user_metadata.full_name || "Usuario"}</strong><small>Cerrar sesión</small></div></button></aside><section className="workspace"><header className="topbar"><div><p className="eyebrow">LABORATORIO</p><h1>{view === "new" ? "Alta de OP y muestra" : showCancelled ? "OPs eliminados" : "Entrada de muestras"}</h1></div>{view === "entries" && !showCancelled && <button className="button primary" onClick={() => setView("new")}>＋ Alta de OP</button>}</header>
    {view === "new" ? <form className="order-form" onSubmit={createEntry}><section className="form-card"><h2>Datos de recepción</h2><p>La OP se genera automáticamente al guardar. La fecha compromiso se calcula a ocho días hábiles desde la recepción.</p><div className="form-grid"><label>Cliente<input required value={client} onChange={(event) => setClient(event.target.value)} /></label><label>Análisis<select value={analysis} onChange={(event) => setAnalysis(event.target.value)}><option value="NOM-001-2021-24H">NOM-001-SEMARNAT-2021 · 24 horas</option><option>NOM-001</option><option>NOM-002</option><option>NOM-003</option><option>Personalizado</option></select></label><label>Número de muestreo<input required value={samplingNumber} onChange={(event) => setSamplingNumber(event.target.value)} /></label><label>Número de muestra<input required value={sampleNumber} onChange={(event) => setSampleNumber(event.target.value)} /></label><label>Muestreador<input value={sampler} onChange={(event) => setSampler(event.target.value)} /></label><label>Cotización<input value={quotation} onChange={(event) => setQuotation(event.target.value)} /></label><label>Fecha de recepción<input required type="date" value={receivedAt} onChange={(event) => setReceivedAt(event.target.value)} /></label></div></section><section className="form-card"><h2>Fechas automáticas</h2><div className="summary-row"><div><span>Fecha compromiso</span><strong>{formatDate(dueDate(receivedAt))}</strong></div><div><span>Número de informe</span><strong>Se asigna al emitir</strong></div></div></section><div className="form-actions"><button type="button" className="button secondary" onClick={() => setView("entries")}>Cancelar</button><button className="button primary" disabled={creating}>{creating ? "Guardando…" : "Registrar entrada"}</button></div></form> : <section className="table-card"><div className="table-toolbar"><div><h2>{showCancelled ? "OPs eliminados" : "Registro de entrada de muestras"}</h2><p>{filteredEntries.length} registros encontrados</p></div>{selectedEntryIds.size > 0 && <>{showCancelled && <button className="button secondary" onClick={() => void runSelection("restore_sample_entry", `¿Restaurar ${selectedEntryIds.size} OP(s)?`)}>Restaurar ({selectedEntryIds.size})</button>}<button className="button danger" onClick={() => void runSelection(showCancelled ? "delete_sample_entry_permanently" : "cancel_sample_entry", showCancelled ? `¿Eliminar definitivamente ${selectedEntryIds.size} OP(s)? Esta acción no se puede deshacer.` : `¿Enviar ${selectedEntryIds.size} OP(s) a eliminados?`)}>{showCancelled ? "Eliminar definitivamente" : "Eliminar OP"} ({selectedEntryIds.size})</button></>}<input aria-label="Buscar entradas" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar OP, cliente o muestra…" /></div><div className="table-wrap"><table className="intake-table"><thead><tr><th><input type="checkbox" aria-label="Seleccionar todas" checked={filteredEntries.length > 0 && selectedEntryIds.size === filteredEntries.length} onChange={toggleAll} /></th><th>OP</th><th>Cliente</th><th>No. muestreo</th><th>Muestreador</th><th>Cotización</th><th>Fecha entrada</th><th>Fecha salida</th><th>Informe</th><th>Análisis</th><th>No. muestra</th></tr></thead><tbody>{filteredEntries.map((entry) => <tr key={entry.id} className={entry.isCancelled ? "cancelled-row" : ""}><td><input type="checkbox" aria-label={`Seleccionar ${entry.op}`} checked={selectedEntryIds.has(entry.id)} onChange={() => toggleEntry(entry.id)} /></td><td><strong>{entry.op}</strong></td><td>{entry.client}</td><td>{entry.samplingNumber}</td><td>{entry.sampler}</td><td>{entry.quotation}</td><td>{entry.received}</td><td><button className="emission-link" onClick={() => openEmission(entry)}>{formatDate(entry.issuedAt)}</button></td><td><button className="emission-link" onClick={() => openEmission(entry)}>{entry.reportNumber || "Registrar"}</button></td><td>{entry.analysis}</td><td><button className="sample-link" onClick={() => void openSample(entry)}>{entry.sampleNumber}</button></td></tr>)}</tbody></table></div></section>}
  </section>{selected && <div className="modal-backdrop" onClick={() => setSelected(null)}><section className="modal detail-modal" onClick={(event) => event.stopPropagation()}><button className="close" onClick={() => setSelected(null)}>×</button><p className="eyebrow">ORDEN DE ANÁLISIS</p>{selected.analysisOrderCreatedAt ? <><div className="detail-grid order-header"><div><span>Número de muestra</span><strong>{selected.sampleNumber}</strong></div><div><span>OP</span><strong>{selected.op}</strong></div><label><span>Fecha de muestreo</span><input type="date" value={sampledInput} onChange={(event) => setSampledInput(event.target.value)} /></label><div><span>Fecha de recepción</span><strong>{selected.received}</strong></div><div><span>Fecha compromiso</span><strong>{selected.due}</strong></div></div>{orderRows.length > 0 ? <><div className="order-template" ref={orderTableRef} onScroll={(event) => syncHorizontalScroll("table", event.currentTarget.scrollLeft)}><div className="order-template-head"><span>Incertidumbre</span><span>Prueba</span><span>Resultados</span><span>Unidades</span><span>Referencia analista</span><span>Fecha</span><span>Analista</span><span>Libera</span></div>{orderRows.map((row) => <div className={row.row_type === "aggregate" ? "order-result-row aggregate-row" : "order-result-row"} key={row.id}><input type="number" min="0" step="0.00001" value={row.uncertainty || ""} onChange={(event) => updateRow(row.id, { uncertainty: event.target.value })} placeholder="0.00000" /><div><strong>{row.label}</strong>{row.aggregation && <small>{row.aggregation}</small>}</div><input value={row.result_value || ""} onChange={(event) => updateRow(row.id, { result_value: event.target.value })} placeholder="Resultado" /><span>{row.unit || dash}</span><input inputMode="numeric" maxLength={5} value={row.analyst_reference || ""} onChange={(event) => updateRow(row.id, { analyst_reference: event.target.value.replace(/\D/g, "") })} placeholder="00000" /><input type="date" value={row.result_date || ""} onChange={(event) => updateRow(row.id, { result_date: event.target.value })} /><input maxLength={3} value={row.analyst_name || ""} onChange={(event) => updateRow(row.id, { analyst_name: event.target.value.toUpperCase() })} placeholder="ABC" /><input maxLength={3} value={row.released_by || ""} onChange={(event) => updateRow(row.id, { released_by: event.target.value.toUpperCase() })} placeholder="ABC" /></div>)}</div><div className="order-bottom-scroll" ref={orderBottomScrollRef} onScroll={(event) => syncHorizontalScroll("bottom", event.currentTarget.scrollLeft)}><div /></div></> : <p className="modal-note">Esta orden no tiene aún una plantilla de parámetros.</p>}<button className="button primary full" disabled={savingOrder} onClick={() => void saveAnalysisOrder()}>{savingOrder ? "Guardando…" : "Guardar orden de análisis"}</button></> : <><h2>{selected.sampleNumber}</h2><p className="modal-note">Aún no se ha generado la orden de análisis para esta muestra.</p><button className="button primary full" disabled={generatingOrder} onClick={() => void generateAnalysisOrder()}>{generatingOrder ? "Generando…" : "Generar orden de análisis"}</button></>}</section></div>}{emissionEntry && <div className="modal-backdrop" onClick={() => setEmissionEntry(null)}><section className="modal" onClick={(event) => event.stopPropagation()}><button className="close" onClick={() => setEmissionEntry(null)}>×</button><p className="eyebrow">EMISIÓN DE INFORME</p><h2>{emissionEntry.op}</h2><p className="client-name">{emissionEntry.client} · Muestra {emissionEntry.sampleNumber}</p><form className="auth-form" onSubmit={saveEmission}><label>Número de informe<input value={reportInput} onChange={(event) => setReportInput(event.target.value)} placeholder="Ej. 001" /></label><label>Fecha de salida<input type="date" value={issuedInput} onChange={(event) => setIssuedInput(event.target.value)} /></label><button className="button primary full" disabled={savingEmission}>{savingEmission ? "Guardando…" : "Guardar emisión"}</button></form></section></div>}</main>;
}
