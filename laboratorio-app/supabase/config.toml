# Local development and CI configuration; remote project selection uses supabase link.
project_id = "laboratorio-app"

[db]
port = 54322
shadow_port = 54320
# Confirm this matches the production project's PostgreSQL major version during setup.
major_version = 17

[db.migrations]
enabled = true

[db.seed]
enabled = false
